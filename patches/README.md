# CVE Patches for RIOT-OS
In this directory, you can find patches that re-expose known CVEs in the RIOT-OS codebase.

## Usage
```bash
# From the root of the RIOT-OS repository
git apply patches/<patch-file>
```
